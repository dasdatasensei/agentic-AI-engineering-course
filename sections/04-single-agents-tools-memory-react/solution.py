"""Section 4 solution — reference implementation.

NOTE TO INSTRUCTOR: this file is intentionally left as a structural
placeholder rather than a fully live-tested implementation, because
run_react_loop() requires a real LLM call (Gemini/Groq) and web_search_tool()
requires a live Tavily key — neither can be verified end-to-end without
your actual credentials. Fill this in against exercise.py's signatures when
you record this lecture; the pattern from sections/03-rag-fundamentals/
solution.py (real, runnable, network-independent logic separated from the
one network-dependent call) is the template to follow.

See exercise.py in this folder for the function signatures to implement.
"""

raise NotImplementedError(
    "Reference solution pending — record against exercise.py once Section 4 "
    "lecture scripts are finalized. See file docstring for guidance."
)
